.env
__pycache__/
*.py[cod]
.venv/
dist/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Coverage outputs
.coverage
.coverage.*

# Local test artifacts
test-results/

# Claude harness scratch state — per-session pid/timestamp lock that
# accidentally lands in commits when `git add -A` is used.
.claude/scheduled_tasks.lock
